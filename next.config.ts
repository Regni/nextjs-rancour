import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["raider.io", "cdn.discordapp.com", "render.worldofwarcraft.com"],
  },
  /* config options here */
};

export default nextConfig;
